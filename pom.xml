<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.lulala</groupId>
    <artifactId>LangChain4j-demo</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>LangChain4j-demo</name>
    <description>LangChain4j-demo</description>
    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <spring.boot3.version>3.5.14</spring.boot3.version>
        <dev.langchain4j.spring>1.17.2-beta27</dev.langchain4j.spring>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-spring-boot-starter</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-open-ai-spring-boot-starter</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-ollama-spring-boot-starter</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- 使用 Flux -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-reactor</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- 多 Agent -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-agentic</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- a2a 客户端 -->
        <!-- LangChain4j Agentic A2A 集成 -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-agentic-a2a</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- a2a 服务端 -->
        <!-- A2A Java SDK Server Common -->
        <dependency>
            <groupId>io.github.a2asdk</groupId>
            <artifactId>a2a-java-sdk-server-common</artifactId>
            <version>0.3.3.Final</version>
        </dependency>
        <!-- Easy RAG -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-easy-rag</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- 中文文档检索使用中文向量模型，避免中文短问题召回失败 -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-embeddings-bge-small-zh-v15</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- Graph Transformer -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-community-llm-graph-transformer</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- 网络搜索内容检索器（Web Search Content Retriever）: tavily -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-web-search-engine-tavily</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- 重排序（Rerank）/语义搜索（Semantic Search）: DashScope/Qwen -->
        <!--
            重排序（Rerank）/语义搜索（Semantic Search）:
                cohere（报403 Forbidden）
                jina（需要梯子（系统代理），中文 RAG demo 更建议换成 Jina reranker 或本地 ONNX reranker）
                DashScope/Qwen（国内可用）
        -->
<!--        <dependency>-->
<!--            <groupId>dev.langchain4j</groupId>-->
<!--            <artifactId>langchain4j-cohere</artifactId>-->
<!--            <version>${dev.langchain4j.spring}</version>-->
<!--        </dependency>-->
<!--        <dependency>-->
<!--            <groupId>dev.langchain4j</groupId>-->
<!--            <artifactId>langchain4j-jina</artifactId>-->
<!--            <version>${dev.langchain4j.spring}</version>-->
<!--        </dependency>-->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-community-dashscope</artifactId>
            <!-- 1.14.1-beta24 版本没有 QwenScoringModel -->
            <version>1.17.0-beta27</version>
        </dependency>
        <!-- _05_Advanced_RAG_with_Metadata_Filtering_Examples.llmGeneratedMetadataFilterExample 示例用 -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-embedding-store-filter-parser-sql</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- _10_Advanced_RAG_SQL_Database_Retreiver_Example 示例用 -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-experimental-sql</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>
        <!-- Guardrails（护栏机制） -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-guardrails</artifactId>
            <version>${dev.langchain4j.spring}</version>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>2.3.232</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
            <version>5.8.46</version>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.18.3</version>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot3.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
                修复 GHSA-72hv-8253-57qq: jackson-core 非阻塞解析器 DoS 漏洞, 强制升级至 2.21.2。
                解决 langchain4j-open-ai-spring-boot-starter 依赖 jackson-core 2.18.3 而存在 GHSA-72hv-8253-57qq 漏洞的问题
            -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>2.21.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <encoding>UTF-8</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot3.version}</version>
                <configuration>
                    <mainClass>com.lulala.langchain4j.LangChain4jDemoApplication</mainClass>
                    <skip>true</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
